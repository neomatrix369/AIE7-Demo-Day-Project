# =============================================================================
# RagCheck - Environment Configuration
# =============================================================================
# Copy this file to .env and update the values as needed
# cp .env.example .env

# =============================================================================
# OpenAI Configuration (Required)
# =============================================================================
# Your OpenAI API key for text embeddings
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# =============================================================================
# Qdrant Vector Database Configuration
# =============================================================================
# Qdrant server URL (default: local Docker instance)
QDRANT_URL=http://localhost:6333

# Qdrant API key (optional for local instances, required for cloud)
# For Vercel
# QDRANT_URL=""
QDRANT_API_KEY=

# Name of the collection to store document vectors
QDRANT_COLLECTION_NAME=student_loan_corpus

# =============================================================================
# Data Configuration
# =============================================================================
# Path to the data folder containing CSV and PDF files
# Relative path from backend directory
DATA_FOLDER=data/

# =============================================================================
# Logging Configuration
# =============================================================================
# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# =============================================================================
# Development Configuration (Optional)
# =============================================================================
# Frontend development server URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Backend server configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Enable development mode features
DEBUG=false

# =============================================================================
# Cloud Deployment Configuration
# =============================================================================
# Vercel frontend URL (for Railway backend CORS)
VERCEL_FRONTEND_URL=https://xxxxx.vercel.app

# Additional frontend domains for CORS (if needed)
# VERCEL_DOMAIN=https://your-custom-domain.com

# =============================================================================
# Vercel Deployment Configuration (Frontend-accessible variables)
# =============================================================================
# Set these variables for Vercel deployment (frontend needs browser access)
NEXT_PUBLIC_DEPLOYMENT_ENV=local

# =============================================================================
# Docker Configuration (Container and Image Names)
# =============================================================================
# Project name prefix for consistent naming
PROJECT_NAME=ragcheck

# Docker Compose project name (controls network and volume naming)
COMPOSE_PROJECT_NAME=${PROJECT_NAME}

# Container names
QDRANT_CONTAINER_NAME=${PROJECT_NAME}-qdrant
BACKEND_CONTAINER_NAME=${PROJECT_NAME}-backend
FRONTEND_CONTAINER_NAME=${PROJECT_NAME}-frontend

# Image names for built services (backend and frontend)
BACKEND_IMAGE_NAME=${PROJECT_NAME}-backend
FRONTEND_IMAGE_NAME=${PROJECT_NAME}-frontend

# External image versions
QDRANT_IMAGE=qdrant/qdrant:latest